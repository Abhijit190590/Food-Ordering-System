{
    "buildCommand": "chmod +x ./node_modules/.bin/ng && ./node_modules/.bin/ng build --configuration production",
    "outputDirectory": "dist/frontend",
    "installCommand": "npm install",
    "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
